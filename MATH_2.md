Sizes & offsets (exact, FIPS-concrete) - SHA-384: 48 B out, 128 B block; ⌈64/48⌉ = 2 ⇒ counters {1,2} ⊂ [1, 2³²−1]; [L]₃₂ = 512 b = 64 B exactly (bits ✓) → SP 800-108r1 CTR framing [i]₃₂ ‖ "PITHOS-1" ‖ 0x00 ‖ H0 ‖ [L]₃₂ = 4‖8‖1‖|H0|‖4. All non-Context fields fixed-width ⇒ total length pins |H0| ⇒ (i, H0) ↦ PRF-input is injective, not merely prefix-free. - K_aead = OKM[0:32] ⊂ K(1); K_commit = K(1)[32:48] ‖ K(2)[0:16]; both 256 b; commitment tag 256 b; nonce 4+8 = 12 B = 96 b GCM-native; tag full 128 b (no truncation) ✓. - FIPS 203/204 concretes: |ct_kem| = |ek| = 1568 B, |ss| = 32 B; |σ| = 4627 B, |pk_S| = 2592 B ⇒ |H0| ≥ 1664 B (ct + two 48-B hashes) + scalars; |sealed| = |H0| + 32 + PL + 16n. SigInput = 18+48+48 = 114 B; labels 8/13/19/18 B. - Parse: |C_i| = min(CS, PL−i·CS) + 16 from H0 alone ⇒ deterministic. **New requirement: enforce |sealed| = |H0|+32+PL+16n exactly.** A parser tolerating trailing bytes admits byte-distinct blobs opening identically — format-level INT-CTXT loss (INT-PTXT intact; Verify catches it via SHA-384(sealed) only when the signature is used). KDF chain - salt = SHA-384(label‖H0) public and message-bound; KDK = HMAC(salt, ss); K(i) per 800-108r1 ⇒ this is precisely SP 800-56Cr2 two-step KDM (extract→expand), sharper than "HKDF-like". |salt| = |KDK| = 48 < 128 B ⇒ no HMAC key pre-hash branch. Acyclic: H0 ∩ {ss, OKM, commitment} = ∅ ✓. - SHA-384 = truncated SHA-512 with distinct IV ⇒ length-extension-immune ⇒ the unkeyed labeled salt is sound. - Hygiene note (new): unkeyed SHA-384 is also used unlabeled (aad_hash, both pk hashes, SHA-384(sealed), SHA-384(AAD)). These are separated by field position, not domain labels; no ?= compares across contexts, so no confusion exists — but labeling every hash would close the class. - Keyed domains: (KDK, "PITHOS-1"), (K_commit, "…commitment"), (sk_S, "…signature"-prefixed message). "PITHOS-1" being a prefix of the other labels is immaterial: it occurs only keyed-by-KDK inside 0x00-delimited framing. Chunking / AEAD budgets - n = max(1, ⌈PL/CS⌉); PL=0 ⇒ n=1, C₀=∅, T₀=16 B ✓. CS=0 undefined — require CS ≥ 1 (unenforced). CS ≤ 2³⁶−32 B from GCM's 2³⁹−256-b cap (unenforced); |aad_i| < 2⁶⁴ b auto-satisfied. - **New totality bound: uint64_be(n) is defined ⇔ n ≤ 2⁶⁴−1 ⇔ PL ≤ CS·(2⁶⁴−1).** Automatic iff PL, CS are uint64 with CS ≥ 1 — but the spec never pins field widths. Pin them. - σ exact (was ≈): σ = 1 + n + Σᵢ⌈|Pᵢ|/16⌉ ≤ ⌊PL/16⌋ + 2n + 1 (subkey H once, E(K,J0) per chunk, CTR blocks). Adv_conf ≲ σ²·2⁻¹²⁸, vacuous as n → 2⁶⁴; enforce σ ≤ 2ᵗ (t = 48 ⇒ margin 2⁻³², PL ≤ 2⁵² B) — unenforced. Per-message keys ⇒ budgets are per-message ✓. SP 800-38D's 2³² cap targets RBG IVs; deterministic construction requires uniqueness only — met. - Online forgery (new line): Adv_forge ≲ q_v(ℓ+1)·2⁻¹²⁸ per key, q_v = adversarial Open attempts — paid per query, unlike the offline costs below. Nonce & key freshness - intra: i ↦ uint64_be(i) injective on [0, n−1]; constant 32-b prefix ⇒ per-key space 2⁶⁴ ⊂ 2⁹⁶ — sufficient given single-message keys, prefix reserved. - inter: Encaps samples m ←$ {0,1}²⁵⁶ with (ss, ct) = f(m, pk) ⇒ repeat ⇔ m-repeat: Pr ≤ q²·2⁻²⁵⁷ over q seals; and ct ∈ H0 means even an ss collision with distinct ct yields distinct salt ⇒ distinct KDK. Bound, not certainty ✓. Stream integrity - aad_i = H0‖commitment‖AAD‖i‖n ⇒ reorder (i), truncate/extend (n, plus PL ∈ H0), parameter swap (H0), cross-message splice (fresh K_aead) all reduce to tag forgery. - Identification (new): the chunk layer instantiates STREAM (HRRV, CRYPTO'15) with (index, total) in AAD in place of (index, last-flag) — binds finality upfront, giving nOAE-style position/termination guarantees; all-or-nothing release additionally removes RUP exposure, at the cost of full buffering (streaming output would need explicit abort semantics). Commitment — two corrections of my prior pass - Split the strength claim: two-sided (both keys adversarial, offline) = birthday on the 256-b tag = 2¹²⁸; one-sided (second opening of an honest blob) = generic target-preimage ≥ 2²⁵⁶, before even requiring K′_commit to arise from Decaps(sk_R, ·) through the KDF. - **Retraction: "lone sub-192-b component" was sloppy — GCM's 128-b tag is also <192 but online per-attempt.** Precise statement: the commitment is the only offline-birthday 2¹²⁸ surface in the design; a full 48-B tag lifts it to 2¹⁹², matching the suite. - Binding chain, direction fixed: violating K_aead-binding requires a collision in ≥1 of {commitment tag (256 b → 2¹²⁸), K_commit (256 b → 2¹²⁸), KDK (384 b → 2¹⁹²)}; equal KDK ∧ equal H0 ⇒ equal OKM ⇒ equal K_aead deterministically. Since both commitment input and every aad_i contain H0 ⊇ {ct, rpk_hash, aad_hash, ids}, this is context commitment (CMT-4-flavor), not bare key commitment. - Ordering ✓ (check precedes loop); FIPS 203 implicit rejection (K̄ = J(z‖c), never ⊥) ⇒ wrong-key decap ⇒ pseudorandom ss ⇒ mismatch ⇒ uniform PITHOS_ERR_AUTH — kills content-based partitioning oracles. Residual is implementation, not math: constant-time compares and uniform rejection latency, else which-check-failed leaks. rpk_hash ?= gates cross-recipient opens at 2¹⁹² before any KEM work; retargeting H0 shifts salt/KDK/commitment/aad simultaneously ⇒ auth fails. Signature - Floor: SHA-384 CR 2¹⁹²; SigInput 114 B; explicit ?= 1 ✓. Sharpened to an asymmetry: the signature binds recipient and exact ciphertext (sealed ⊇ H0 ⊇ rpk_hash, ct, aad_hash), but nothing on the encryption side binds the signer. Claiming attack stands (classic defective encrypt-then-sign): strip σ, re-sign sealed_bytes under pk_A; Verify(pk_A, ·) = 1 for content A never read. signer_key_hash ?= H(pk_S) authenticates the asserted key only. Fix: signer_key_hash ∈ H0 ⇒ it then flows into salt, KDK, commitment, every aad_i, and the signed hash — bidirectional binding. Freshness/replay: out of scope for data-at-rest by design. Symmetry / downgrade - Every ?= recomputes Seal's value from authenticated inputs; n unstated in Open but n = f(H0) and bound in every aad_i ⇒ presentational only. profile and all *_id fields are bound 5×: salt, KDF context, commitment input, each aad_i, and SHA-384(sealed) under σ. Cheap gates (profile, rpk_hash, aad_hash) precede Decaps ✓. Assumption ledger & proof route (new — what the ✓ marks are conditional on) - ML-KEM-1024 IND-CCA (MLWE + FO with implicit rejection); HMAC-SHA384 as extractor (dual-PRF) and as PRF; SHA-384 CR at 2¹⁹²; AES-256 PRP (multi-user margin ample); GCM bounds as above; ML-DSA-87 EUF-CMA. - A proof would be standard-shaped: IND-CCA KEM + one-time AE DEM ⇒ IND-CCA hybrid (KEM-DEM), with DEM = STREAM-style chunked GCM under header-bound 56Cr2 derivation, plus the commitment layer for robustness. Route identified ≠ proof written. Verdict: all prior arithmetic survives re-audit (2-block expand, bits-vs-bytes L, offsets, 114 B, 96-b nonce, injective framing). Delta over last pass — two self-corrections (offline/online commitment precision replacing "sub-192"; binding restated as an explicit weakest-link collision chain) and five additions (uint64(n) totality bound; strict total-length parse; exact σ; STREAM/nOAE + KEM-DEM identification with assumption ledger; unlabeled-hash hygiene). Standing spec gaps: CS ∈ [1, 2³⁶−32], σ-budget, n-width, strict-length parse, signer binding — none arithmetic.